# 🚀 راهنمای سریع Deployment

این راهنما برای کسانی است که می‌خواهند به سرعت پروژه را deploy کنند.

## ⚡ روش سریع (3 مرحله)

### 1️⃣ اجرای اسکریپت Deployment

```bash
npm run deploy
```

یا با pnpm:
```bash
pnpm deploy
```

### 2️⃣ بررسی پوشه deploy

پس از اجرای اسکریپت، پوشه `deploy` در ریشه پروژه ایجاد می‌شود که شامل تمام فایل‌های آماده برای سرور است.

### 3️⃣ آپلود به سرور

```bash
# آپلود فایل‌ها
scp -r deploy/* user@server:/path/to/app

# یا ایجاد ZIP و آپلود
npm run deploy:zip
# سپس آپلود فایل ZIP
```

---

## 📦 ایجاد فایل ZIP (اختیاری)

برای ایجاد فایل ZIP از پوشه deploy:

```bash
npm run deploy:zip
```

این دستور ابتدا deploy را انجام می‌دهد و سپس فایل ZIP ایجاد می‌کند.

---

## 🖥️ نصب و اجرا روی سرور

### مراحل سریع:

```bash
# 1. رفتن به پوشه پروژه
cd /path/to/app

# 2. نصب dependencies
pnpm install --production

# 3. تنظیم متغیرهای محیطی
cp .env.example .env.local
# ویرایش .env.local با مقادیر واقعی

# 4. بیلد (در صورت نیاز)
pnpm build

# 5. اجرا
pnpm start
```

### با PM2 (پیشنهادی):

```bash
# نصب PM2
npm install -g pm2

# اجرا
pm2 start npm --name "viramap-website" -- start

# ذخیره
pm2 save
pm2 startup
```

---

## ✅ چک‌لیست سریع

- [ ] اسکریپت deploy اجرا شد
- [ ] پوشه deploy بررسی شد
- [ ] فایل .env.local تنظیم شد
- [ ] فایل‌ها روی سرور آپلود شدند
- [ ] dependencies نصب شدند
- [ ] پروژه اجرا شد

---

## 🆘 مشکل دارید؟

برای راهنمای کامل، فایل `DEPLOYMENT.md` را مطالعه کنید.

---

**زمان تقریبی:** 5-10 دقیقه

