# 🔧 راهنمای تنظیم متغیرهای محیطی

## فایل `.env.local`

در ریشه پروژه فایل `.env.local` ایجاد کنید و محتوای زیر را اضافه کنید:

```env
# URL اصلی سایت (برای SEO و Structured Data)
NEXT_PUBLIC_SITE_URL=https://viramaps.ir
```

**نکته مهم:**
- فایل `.env.local` در `.gitignore` است و به repository اضافه نمی‌شود
- URL را با آدرس واقعی سایت خود جایگزین کنید
- برای محیط production، این متغیر را در تنظیمات hosting خود هم اضافه کنید

## استفاده در کد

این متغیر در فایل‌های زیر استفاده می‌شود:
- `app/layout.tsx` - برای metadataBase
- `app/sitemap.ts` - برای URLهای sitemap
- `components/lib/structured-data.tsx` - برای Structured Data URLs

## بررسی

پس از ایجاد فایل، مطمئن شوید که:
1. فایل در ریشه پروژه است (هم سطح با `package.json`)
2. نام فایل دقیقاً `.env.local` است
3. URL صحیح است
4. بعد از ایجاد فایل، سرور را restart کنید

