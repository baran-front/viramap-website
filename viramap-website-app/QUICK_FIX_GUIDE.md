# ⚡ راهنمای سریع رفع خطاهای Runtime

## 🎯 خلاصه تغییرات

تمام مشکلات شناسایی شده و برطرف شده‌اند. برای اعمال تغییرات، مراحل زیر را دنبال کنید:

---

## 🚀 روش سریع (پیشنهادی)

### گزینه 1: استفاده از اسکریپت خودکار
```bash
pnpm fix-runtime
# یا
npm run fix-runtime
```

این اسکریپت به صورت خودکار:
- ✅ `node_modules` را حذف می‌کند
- ✅ Cache را پاک می‌کند
- ✅ Dependencies را نصب می‌کند
- ✅ Build را تست می‌کند

---

### گزینه 2: انجام دستی

```bash
# 1. حذف node_modules و cache
rm -rf node_modules .next
rm -f pnpm-lock.yaml

# 2. نصب مجدد
pnpm install
# یا
npm install

# 3. تست build
pnpm build
```

---

## ✅ تغییرات اعمال شده

### 1. React 19 → React 18.3.1
- **علت:** Next.js 16.0.5 از React 18 پشتیبانی می‌کند
- **تأثیر:** رفع خطاهای ناسازگاری Runtime

### 2. Zod 4.1.13 → Zod 3.24.1
- **علت:** نسخه 4 وجود ندارد
- **تأثیر:** رفع خطاهای validation

### 3. @hookform/resolvers 5 → 3.9.1
- **علت:** سازگاری با Zod 3
- **تأثیر:** رفع خطاهای فرم

### 4. فعال‌سازی Turbopack
- **تغییر:** `dev` script از `--webpack` به `--turbopack`
- **تأثیر:** بهبود سرعت development

### 5. اضافه کردن "use client" به hooks
- **فایل:** `components/lib/hooks/useArticles.ts`
- **تأثیر:** رفع خطاهای Server/Client boundary

---

## 📋 چک‌لیست پس از نصب

پس از اجرای دستورات بالا، بررسی کنید:

- [ ] `pnpm list react` → باید `18.3.1` باشد
- [ ] `pnpm list zod` → باید `3.24.1` باشد
- [ ] `pnpm build` → باید بدون خطا باشد
- [ ] `pnpm dev` → باید بدون خطا اجرا شود

---

## 🐛 اگر هنوز خطا دارید

### خطای 1: "Cannot find module"
```bash
pnpm install
```

### خطای 2: "Invalid hook call"
- بررسی کنید که تمام کامپوننت‌های استفاده‌کننده از hooks دارای `"use client"` هستند

### خطای 3: خطاهای TypeScript
```bash
pnpm install -D @types/react@^18.3.18 @types/react-dom@^18.3.5
```

### خطای 4: خطاهای Turbopack در Windows
```bash
# استفاده از Webpack به جای Turbopack
pnpm dev:webpack
```

---

## 📚 مستندات کامل

برای اطلاعات بیشتر، فایل `RUNTIME_ERRORS_FIX.md` را مطالعه کنید.

---

## ✨ پس از رفع خطاها

پروژه شما باید:
- ✅ بدون خطا build شود
- ✅ بدون خطا در development اجرا شود
- ✅ با Turbopack سریع‌تر کار کند
- ✅ با React 18 پایدار باشد

---

**زمان تقریبی:** 5-10 دقیقه

**نیاز به کمک؟** فایل `RUNTIME_ERRORS_FIX.md` را مطالعه کنید.

