# 📝 راهنمای گام‌به‌گام - بیلد و ارسال برای بکند

این راهنما دقیقاً به شما می‌گوید چه کار کنید، قدم به قدم.

---

## ✅ مرحله 1: باز کردن ترمینال

### در VS Code:
- کلید `Ctrl + ~` را بزنید
- یا از منو: `Terminal` → `New Terminal`

### یا PowerShell/CMD را باز کنید

---

## ✅ مرحله 2: رفتن به پوشه پروژه

در ترمینال تایپ کنید:

```bash
cd "C:\Users\Fargang\Documents\GitHub\viramap-website\viramap-website-app"
```

یا اگر در VS Code هستید و پروژه باز است، ترمینال خودش در پوشه پروژه است.

---

## ✅ مرحله 3: بررسی اینکه در پوشه درست هستید

این دستور را بزنید:

```bash
dir
```

باید فایل `package.json` را ببینید.

---

## ✅ مرحله 4: اجرای اسکریپت Deployment

این دستور را بزنید:

```bash
pnpm deploy:zip
```

**صبر کنید!** این کار ممکن است 2-5 دقیقه طول بکشد.

### چه اتفاقی می‌افتد:
1. ✅ Dependencies نصب می‌شوند (اگر نیاز باشد)
2. ✅ پروژه بیلد می‌شود
3. ✅ فایل‌های ضروری جمع‌آوری می‌شوند
4. ✅ پوشه `deploy` ایجاد می‌شود
5. ✅ فایل ZIP ایجاد می‌شود

### اگر خطا گرفتید:
- خطا را بخوانید
- معمولاً مشکل از نبود dependencies است
- اول این را بزنید: `pnpm install`
- بعد دوباره: `pnpm deploy:zip`

---

## ✅ مرحله 5: بررسی فایل ZIP

بعد از اتمام، باید یک فایل ZIP ببینید که نامش شبیه این است:

```
deploy-2024-01-15T10-30-00.zip
```

### پیدا کردن فایل:
1. در VS Code: در Explorer سمت چپ، فایل ZIP را ببینید
2. یا در File Explorer ویندوز: پوشه پروژه را باز کنید

---

## ✅ مرحله 6: بررسی حجم فایل

فایل ZIP را راست کلیک کنید → `Properties` → حجم را ببینید

- حجم معمول: 10-50 MB
- اگر خیلی بزرگ است (مثلاً 200+ MB): ممکن است `node_modules` داخلش باشد (اشکال دارد!)

---

## ✅ مرحله 7: ارسال فایل برای بکند

### روش 1: آپلود در Google Drive / Dropbox
1. فایل ZIP را آپلود کنید
2. لینک اشتراک‌گذاری را برای بکند بفرستید

### روش 2: ارسال مستقیم
- از طریق ایمیل
- از طریق پیام‌رسان (Telegram, WhatsApp, etc.)
- از طریق WeTransfer

---

## ✅ مرحله 8: اطلاعات برای بکند

این متن را برای بکند بفرستید:

```
سلام، فایل بیلد پروژه آماده است.

فایل ZIP را extract کنید و این دستورات را اجرا کنید:

1. Extract فایل ZIP
2. cd deploy
3. pnpm install --production
4. cp .env.example .env.local
5. فایل .env.local را با مقادیر واقعی پر کنید
6. pnpm start

پورت پیش‌فرض: 3000
Node.js مورد نیاز: نسخه 18 یا بالاتر
pnpm مورد نیاز: نسخه 8 یا بالاتر
```

---

## 🔍 بررسی نهایی

قبل از ارسال، این موارد را چک کنید:

- [ ] فایل ZIP ایجاد شده است
- [ ] حجم فایل منطقی است (10-50 MB)
- [ ] نام فایل شامل تاریخ و زمان است
- [ ] فایل ZIP در پوشه اصلی پروژه است

---

## ❌ اگر مشکلی پیش آمد

### مشکل: خطای "npm: command not found"
**راه‌حل:** Node.js نصب نیست. از [nodejs.org](https://nodejs.org) نصب کنید.

### مشکل: خطای "Cannot find module"
**راه‌حل:** 
```bash
pnpm install
```
بزنید و دوباره `pnpm deploy:zip` را اجرا کنید.

### مشکل: خطای بیلد
**راه‌حل:** 
- خطا را بخوانید
- معمولاً مشکل از کد است
- خطا را برطرف کنید و دوباره بیلد کنید

### مشکل: فایل ZIP خیلی بزرگ است
**راه‌حل:** 
- بررسی کنید که `node_modules` در ZIP نیست
- اگر هست، اسکریپت deploy مشکل دارد

---

## 📞 اگر هنوز مشکل دارید

این اطلاعات را بدهید:
1. خطای دقیق که می‌بینید
2. نسخه Node.js: `node --version`
3. نسخه pnpm: `pnpm --version`

---

**زمان تقریبی کل:** 5-10 دقیقه

**مرحله مهم:** صبر کنید تا اسکریپت کامل شود! ⏳

