# 🚀 راهنمای بیلد و ارسال برای بکند

این راهنما به شما کمک می‌کند پروژه را بیلد کنید و فایل‌های آماده را برای تیم بکند ارسال کنید.

## ⚡ روش سریع (پیشنهادی)

### مرحله 1: اجرای اسکریپت Deployment

```bash
pnpm deploy:zip
```

این دستور به صورت خودکار:
- ✅ پروژه را بیلد می‌کند
- ✅ فایل‌های ضروری را جمع‌آوری می‌کند
- ✅ پوشه `deploy` را ایجاد می‌کند
- ✅ فایل ZIP ایجاد می‌کند

### یا به صورت جداگانه:

```bash
# فقط deploy
pnpm deploy

# فقط ایجاد ZIP (بعد از deploy)
node scripts/create-zip.js
```

این دستور یک فایل ZIP با نام `deploy-YYYY-MM-DDTHH-MM-SS.zip` در ریشه پروژه ایجاد می‌کند.

---

## 📦 چه فایل‌هایی باید بفرستید؟

### گزینه 1: فایل ZIP (پیشنهادی)

فقط فایل ZIP ایجاد شده را بفرستید:
```
deploy-2024-01-15T10-30-00.zip
```

### گزینه 2: پوشه deploy

اگر نمی‌خواهید ZIP کنید، کل پوشه `deploy` را بفرستید.

---

## 📋 محتوای پوشه Deploy

پس از اجرای `pnpm deploy`، پوشه `deploy` شامل موارد زیر است:

```
deploy/
├── .next/              # فایل‌های بیلد شده Next.js (مهم!)
├── app/                # صفحات و Route ها
├── components/         # کامپوننت‌های React
├── public/             # فایل‌های استاتیک (تصاویر، فونت‌ها)
├── services/           # سرویس‌های API
├── package.json        # وابستگی‌های پروژه
├── next.config.ts      # تنظیمات Next.js
├── tsconfig.json       # تنظیمات TypeScript
├── postcss.config.mjs  # تنظیمات PostCSS
├── .env.example        # نمونه فایل متغیرهای محیطی
└── README.md           # راهنمای نصب و اجرا
```

---

## 📝 اطلاعاتی که باید به بکند بدهید

### 1. فایل‌های مورد نیاز

- ✅ فایل ZIP یا پوشه `deploy`
- ✅ فایل `.env.example` (که در deploy قرار دارد)

### 2. دستورات نصب و اجرا

```bash
# 1. Extract فایل ZIP (اگر ZIP فرستاده‌اید)
unzip deploy-YYYY-MM-DDTHH-MM-SS.zip
cd deploy

# 2. نصب dependencies
pnpm install --production
# یا اگر pnpm ندارید:
# npm install --production

# 3. تنظیم متغیرهای محیطی
cp .env.example .env.local
# سپس فایل .env.local را با مقادیر واقعی پر کنید

# 4. بیلد (در صورت نیاز - معمولاً نیازی نیست چون قبلاً بیلد شده)
pnpm build
# یا: npm run build

# 5. اجرا
pnpm start
# یا: npm start
```

### 3. متغیرهای محیطی مورد نیاز

به بکند بگویید که باید فایل `.env.local` را از `.env.example` ایجاد کند و مقادیر زیر را تنظیم کند:

```env
# مثال - مقادیر واقعی را باید بکند تنظیم کند
NEXT_PUBLIC_API_URL=https://api.example.com
DATABASE_URL=your_database_url
# ... سایر متغیرها
```

### 4. پورت پیش‌فرض

- پورت پیش‌فرض: **3000**
- برای تغییر: `PORT=3001 pnpm start` یا `PORT=3001 npm start`

### 5. نسخه‌های مورد نیاز

- **Node.js**: نسخه 18 یا بالاتر
- **pnpm**: نسخه 8 یا بالاتر (اگر از pnpm استفاده می‌کنید)
- بررسی: 
  ```bash
  node --version
  pnpm --version
  ```

---

## 🔍 بررسی قبل از ارسال

قبل از ارسال فایل‌ها، مطمئن شوید:

- [ ] اسکریپت `deploy` بدون خطا اجرا شده است
- [ ] پوشه `deploy` ایجاد شده است
- [ ] فایل `.next` در پوشه deploy وجود دارد
- [ ] فایل `package.json` در پوشه deploy وجود دارد
- [ ] فایل `.env.example` در پوشه deploy وجود دارد
- [ ] فایل‌های `.env*.local` در deploy نیستند (امنیت)
- [ ] فایل‌های `.git` در deploy نیستند

---

## 🚨 نکات مهم

### ✅ باید بفرستید:
- پوشه `deploy` یا فایل ZIP آن
- فایل `.env.example`

### ❌ نباید بفرستید:
- پوشه `node_modules` (بکند خودش نصب می‌کند)
- فایل‌های `.env*.local` (امنیت!)
- پوشه `.git`
- فایل‌های `.log`
- پوشه `.next` از ریشه پروژه (فقط `.next` داخل `deploy`)

---

## 📤 روش‌های ارسال

### 1. آپلود مستقیم
- Google Drive
- Dropbox
- WeTransfer
- یا هر سرویس اشتراک‌گذاری فایل

### 2. Git Repository
```bash
# ایجاد یک branch جدید برای deployment
git checkout -b deploy/backend
git add deploy/
git commit -m "Add deployment files for backend"
git push origin deploy/backend
```

### 3. FTP/SFTP
اگر دسترسی مستقیم به سرور دارید:
```bash
scp -r deploy/* user@server:/path/to/app
```

---

## 🛠️ عیب‌یابی

### مشکل: فایل ZIP خیلی بزرگ است

**راه‌حل:**
- بررسی کنید که `node_modules` در ZIP نیست
- بررسی کنید که `.next/cache` در ZIP نیست

### مشکل: بکند می‌گوید فایل‌ها کامل نیست

**راه‌حل:**
- مطمئن شوید که اسکریپت `deploy` کامل اجرا شده است
- بررسی کنید که پوشه `.next` در `deploy` وجود دارد

### مشکل: خطا در بیلد روی سرور بکند

**راه‌حل:**
- مطمئن شوید Node.js نسخه 18+ نصب است
- بررسی کنید که تمام dependencies نصب شده‌اند
- بررسی لاگ‌های خطا

---

## 📞 تماس با بکند

اگر سوالی دارید، این اطلاعات را به بکند بدهید:

1. **نسخه Node.js شما**: `node --version`
2. **Package Manager استفاده شده**: pnpm/npm/yarn
3. **حجم فایل ZIP**: (می‌توانید بعد از ایجاد ZIP ببینید)
4. **هر خطای خاص**: اگر در بیلد خطایی دیدید

---

## ✅ چک‌لیست نهایی

قبل از ارسال:

- [ ] `npm run deploy` اجرا شد و موفق بود
- [ ] پوشه `deploy` بررسی شد
- [ ] فایل ZIP ایجاد شد (در صورت نیاز)
- [ ] فایل `.env.example` موجود است
- [ ] فایل‌های حساس حذف شدند
- [ ] حجم فایل مناسب است
- [ ] اطلاعات لازم به بکند داده شد

---

**زمان تقریبی:** 5-10 دقیقه

**حجم تقریبی:** بستگی به پروژه دارد (معمولاً 10-50 MB)

